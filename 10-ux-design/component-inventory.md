# 元件清單

> 既有元件清單，AI 在生成 UI 前必須先查詢此清單，避免重造輪子。

## 基礎元件

| 元件 | 來源 | 用途 |
|------|------|------|
| Button | 待補 | 主要按鈕 |
| Input | 待補 | 文字輸入 |
| Modal | 待補 | 彈窗 |

## 複合元件

待補。

## 禁止自製清單

如果需求對應到下表元件，**必須**使用既有元件而非重新實作：

待補。
