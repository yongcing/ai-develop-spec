# Spec Audit — yongcing/ai-develop-spec @ a72f679

> Auto-generated by general-purpose sub-agent, 2026-05-22.
> Read-only audit. Findings grouped by severity.

## CRITICAL (blocks AI from completing tasks correctly)

- **DoD tells AI to use the wrong package manager and the wrong build tool** — `40-quality-gates/definition-of-done.md:15` and `:24`
  - Says `pnpm lint && pnpm typecheck && pnpm test` for the frontend, and `./gradlew check` for the backend.
  - Every other doc pins **npm** (frontend `tech-stack.md:18`, `ai-generation-rules.md:94`, `local-toolchain.md:34`) and **Maven** (backend `tech-stack.md:19`, `ai-generation-rules.md:92` uses `./mvnw verify`, `local-toolchain.md:53,75`). An AI running `definition-of-done.md` verbatim will issue commands that don't exist in the project and then "fix" the discrepancy by either adding pnpm/gradle wrappers or fudging the check.
  - Fix: change to `npm run lint && npm run typecheck && npm run test` and `./mvnw verify` (matching `ai-generation-rules.md`). Pick one and propagate.

- **`prompts/frontend-feature.md` mandates a library that is forbidden by tech-stack** — `prompts/frontend-feature.md:30`
  - Deliverable list says "TanStack Query hooks 處理 server state".
  - Frontend `tech-stack.md:37-39` and `ai-generation-rules.md:18,20,45` mandate **RTK Query** and explicitly forbid hand-written fetch hooks. The frontend prompt is the AI's primary entry point; if it follows the prompt, it will install `@tanstack/react-query` and produce code that fails review.
  - Fix: replace with "RTK Query hooks (`useGetXxxQuery` / `useXxxMutation`) generated via `@rtk-query/codegen-openapi`".

- **`prompts/frontend-feature.md` mandates a forbidden folder layout** — `prompts/frontend-feature.md:29`
  - Says `src/features/<name>/{components, hooks, api, types}`.
  - `ai-generation-rules.md:33` and `coding-standards.md` define `{components, hooks, schemas, types}` — no `api/` folder; API code lives under top-level `src/api/{generated,extended}/` (`ai-generation-rules.md:83-86`). `system-architecture.md:50-53` does mention `features/<x>/api/` inline, contradicting `ai-generation-rules.md`.
  - Fix: align all three docs. The strictest is `ai-generation-rules.md`; update prompt and architecture diagram to drop `features/<x>/api/` and reference `src/api/extended/`.

- **AI routing table promises a workflow doc that no longer enforces what's claimed** — `CLAUDE.md:19`
  - Row says: "**強制**：每個 UI section 動工前先確認對應 `design/sections/<section>/visual/default.png` + `.measurements.md` 存在".
  - The filename `.measurements.md` (with leading dot) is wrong. Every other doc (`design-to-code-workflow.md:87`, `visual-parity-workflow.md:19,89`, `interaction-spec-workflow.md`, lint script line 73) uses `visual/measurements.md` (no leading dot). The lint script will never find `.measurements.md` and AI told to "check for `.measurements.md`" will either invent it or report "missing" against the wrong path.
  - Fix: `design/sections/<section>/visual/measurements.md`.

## HIGH (likely to produce wrong output)

- **Routing table omits multiple docs AI is supposed to read** — `CLAUDE.md:11-19`
  - For "後端 API/服務" the table lists `tech-stack.md + ai-generation-rules.md + api-contract-rules.md` but not `layering-rules.md`, `error-handling.md`, `security-baseline.md`, or `coding-standards.md`. The `prompts/backend-endpoint.md` correctly lists all six. So an AI obeying `CLAUDE.md` directly skips four critical spec docs (e.g. Problem Details format, `@PreAuthorize` rules, Lombok policy).
  - Symmetrically the frontend row omits `coding-standards.md` and `design-principles.md`.
  - Fix: either expand the routing table to list every must-read, or change the table to point at the corresponding `prompts/*.md` files which already aggregate the list.

- **Two contradictory definitions of `measurements.md` content** — `visual-parity-workflow.md:42-81` vs `design-to-code-workflow.md:95-107` vs `design-tokens.json`
  - `design-to-code-workflow.md:104` example says "StatCard: padding 20px 24px, label 11px uppercase #6e6e73, value 32px 600". Hard-coding the hex `#6e6e73` directly contradicts the same file's line 106 "全部從 design-tokens.json 引用；不准 hardcode" and `ai-generation-rules.md:52` (no `bg-[#3366ff]`).
  - Also `visual-parity-workflow.md:71` says DataTable row height is **48px**, while `design-to-code-workflow.md:105` example says **64px**. Same component, two different "measured" values across the two example specs — an AI looking for guidance will pick one arbitrarily.
  - Fix: example must reference token names (`color.neutral.700` etc.); unify the DataTable example value or note that examples are illustrative only.

- **Lint script does not enforce several rules the docs say it enforces** — `10-ux-design/tools/lint-design-spec.mjs`
  - `interaction-spec-workflow.md:194` claims the lint validates that `interactions.md` is a "合法 markdown table". The script only checks for any `|...|...|` line (lint-design-spec.mjs:104), which matches inline pipes in prose.
  - `interaction-spec-workflow.md:198` claims orphan screenshots are "warn-level"; the script (line 149) actually counts them as failures.
  - `design-to-code-workflow.md:177` requires "規格內**沒有「TBD」「待補」**未連結 issue" — but the lint forbids those tokens unconditionally with no "linked issue" escape hatch.
  - `interaction-spec-workflow.md:213` invokes the lint with path `spec/10-ux-design/tools/lint-design-spec.mjs` (note the `spec/` prefix) which presumes a fixed submodule path.
  - Fix per item: tighten the table regex; document `(no visual change)` as the only valid non-screenshot State; reconcile "warn vs fail" wording; either drop the "未連結 issue" escape hatch or implement it; make the CI example path a placeholder.

- **CI codegen references a tool that does not exist** — `interaction-spec-workflow.md:220` and `design-to-code-workflow.md:226`
  - Both reference `tools/codegen-e2e.mjs` / `tools/codegen-e2e.ts` and `npm run codegen:e2e`. The repo only ships `lint-design-spec.mjs`, `capture-prototype-screens.ts`, `capture-mvp-screens.ts`. `package.json` has no `codegen:e2e` script.
  - AI asked to "run codegen" will hallucinate the tool's contract.
  - Fix: mark as "(planned — not yet shipped)" with clear contract.

- **`visual-diff.ts` is referenced as if it exists** — `design-to-code-workflow.md:218` and 283
  - Block diagram mentions `tools/visual-diff.ts` running Playwright + pixel diff, but no such file exists. DoD checkbox `40-quality-gates/definition-of-done.md:20` says "Playwright pixel diff < 2%" implying the tool is in place.
  - Fix: label as planned, or document the project-side implementation contract.

- **`SecurityFilterChain` rules contradict between `api-contract-rules.md` and `security-baseline.md`** — `api-contract-rules.md:38-40` vs `security-baseline.md:17`
  - api-contract-rules says service-to-service JWT lifetime ≤ 5 minutes.
  - security-baseline says ≤ 15 分鐘.
  - Hard contradiction.
  - Fix: pick one ceiling (recommended: ≤ 5 minutes).

- **Page-size defaults conflict** — `api-contract-rules.md:21` (default 50, max 200) vs `30-backend/ai-generation-rules.md:26` (max page size 100)
  - Same setting, two different ceilings.
  - Fix: align (recommended: default 50, max 100).

## MEDIUM (will eventually cause drift)

- **ADR-0001 destination paths differ from layering rules** — `00-architecture/adr/0001-spring-modulith.md:70-73`
  - Says: "AI 必須將新功能放入特定 module（`com.company.app.<module>.{api,domain,infrastructure}`）".
  - `30-backend/layering-rules.md` and `ai-generation-rules.md:9` use `com.company.<module>` (no `.app`). `coding-standards.md:12` uses `com.company.app.order.domain`. Three different package roots across three docs.

- **Layering: Controller location ambiguous** — `30-backend/layering-rules.md:35` ("api/  (Controller + DTO + 對外 event/SPI)") vs `ai-generation-rules.md:9` (module = `{api, domain, infrastructure}`) vs `system-architecture.md:32-36` ("controller → application service → domain → repository")
  - Is a controller in `api/` or in `controller/`? Three layouts coexist.

- **Stale prototype source pointer** — `10-ux-design/design-tokens.json:3`
  - `$source` claims "Extracted from prototypes/V0_Covenant Data Platform.html" — deleted in a72f679.
  - Fix: change `$source` to neutral description.

- **Component inventory references deleted ADR** — `10-ux-design/component-inventory.md:58,74`
  - Lists `QueryBuilder` as P2 component. ADR-0002 was deleted in this commit.
  - Fix: remove the row or add "implementation deferred".

- **`design-principles.md:53` cites a tool nothing invokes**
  - "CI 用 color-contrast-checker 跑". No CI in this repo, no dep.
  - Fix: ship invocation or rephrase as consumer-side requirement.

- **`design-principles.md` references both old and new prototype workflow paths** — `design-principles.md:110,116`
  - Both paths exist; duplication invites drift.

- **`screens/<section>.measurements.md` path wrong** — `10-ux-design/visual-parity-workflow.md:126`
  - "生成 UI section 前必先讀對應 `screens/<section>.measurements.md`".
  - Everywhere else uses `design/sections/<section>/visual/measurements.md`.

- **DoD requires Flyway but `database-selection.md` permits Mongock** — `40-quality-gates/definition-of-done.md:27`
  - "DB 變更有 Flyway migration" with no MongoDB carve-out.
  - Fix: "DB schema 變更有對應的 migration (Flyway / Mongock)".

- **Backend prompt deliverable list omits Mongock** — `prompts/backend-endpoint.md:30-34`
  - "Entity + Flyway migration" implies SQL only.

- **`design-to-code-workflow.md:279` lists "interaction-spec-workflow.md (TODO)"** but that file now exists.
  - Fix: drop "(TODO)" and link the file.

- **`api-contract-rules.md:54` vs `system-architecture.md:51-52`** — RTK Query generated path and feature-slice api/ described in two locations with subtle overlap.

## LOW / NITS

- `design-tokens.json:7` token comment "NOT Apple blue" — unenforceable.
- `local-toolchain.md:16` Node version pin spelled differently across files.
- `design-principles.md:73` mentions Cypress + Vitest + axe-core; rest of workflow only references Playwright. **Actually closer to HIGH** — is E2E tool Cypress or Playwright?
- `30-backend/ai-generation-rules.md:84` and DoD: `./mvnw verify` vs `./gradlew check` mismatch.
- `adr/0000-template.md`: file-naming guidance missing.
- `README.md:21`: submodule mount-point not declared canonically; CI snippets assume `spec/`.

## Cross-cutting observations

1. **Three locations for "what a module looks like" need consolidation.** `system-architecture.md`, `30-backend/layering-rules.md`, `30-backend/ai-generation-rules.md`, `00-architecture/adr/0001-spring-modulith.md`, and `30-backend/coding-standards.md` each give a slightly different package shape and a slightly different list of layer names. Pick one canonical shape, put it in `layering-rules.md` only, have everything else link to it.

2. **The `CLAUDE.md` routing table is a load-bearing artifact that has fallen behind the prompts/.** AI follows CLAUDE.md to pick which specs to read. The `prompts/*.md` files actually list every must-read spec correctly. Either (a) point each row at the corresponding `prompts/*.md`, or (b) auto-generate CLAUDE.md from the prompts.

3. **Tooling is half-promised across many docs.** `lint:design-spec` exists. `codegen-e2e`, `visual-diff`, `lint:contrast`, "CI rerun prototype capture weekly" do not. The spec presents them as ambient infrastructure. Add a clear "Not yet implemented" tag to every tool reference whose script does not exist.

4. **Frontend stack has a subtle bait-and-switch around server state.** `tech-stack.md` and `ai-generation-rules.md` are emphatic about RTK Query. But `prompts/frontend-feature.md` says TanStack Query. AI scaffolding a feature reads the prompt first and lands on TanStack. Add explicit "never use TanStack Query / SWR / react-query" to `ai-generation-rules.md MUST NOT`.

5. **Cleanup pass after a72f679 is incomplete.** Token source still cites the deleted Covenant prototype; `QueryBuilder` still listed though its ADR was removed; `design-to-code-workflow.md:279` still labels interaction-spec-workflow as TODO though file exists. Sweep for: "Covenant", "covenant", "0002", "data-contract", "QueryBuilder", "(TODO)".
